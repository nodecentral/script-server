This folder is bind-mounted to /app/data inside the container (see docker-compose.yml).
Drop files here to browse/act on them via the "File Info" example runner, or use it as the
persistent storage base for your own scripts.
